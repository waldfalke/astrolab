# [TODO][TASK][P1] L402-APR-003 — Adapt client-agent for real Lightning payment

## Goal
Переписать client-agent для реальной оплаты L402 через lnd-client.

## Problem Statement
Текущий client-agent использует dummy preimage. Нужна реальная оплата Lightning invoice, полученного от Aperture.

## Scope
1. **Обновить `client-agent/client.js`:**
   - Подключиться к lnd-client через gRPC (tls.cert + macaroon из volume)
   - GET → Aperture → получить 402 + WWW-Authenticate
   - Распарсить macaroon и invoice из заголовка
   - Оплатить invoice через `sendPaymentSync` на lnd-client
   - Получить preimage из ответа
   - Повторить запрос с `Authorization: L402 <macaroon>:<preimage>`
   - Получить ресурс
2. **Обновить `client-agent/package.json`** — добавить `@grpc/grpc-js`, `@grpc/proto-loader`
3. **Обновить `client-agent/Dockerfile`** — копировать protos

### Альтернатива (refined-element MCP)
Вместо ручного gRPC можно использовать refined-element/lightning-enable-mcp с его `access_l402_resource`. Оценить трудоёмкость обоих подходов при старте задачи.

## Depends on
- L402-APR-001 (Aperture работает, отдаёт 402)
- L402-APR-002 (lnd-client с funded каналом)

## Done Definition
1. `docker compose run --build client-agent` проходит полный цикл.
2. В логах видно: 402 → pay → preimage → resource.
3. Оплата реальная (видна в `lncli listpayments` на lnd-client и `lncli listinvoices` на lnd).
