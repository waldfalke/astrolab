# Task Log — L402-MVP-006: Integrate Real Lightning Node (LND)

**Date:** 2026-03-06
**Status:** DONE

## Objective

Заменить dummy L402-логику в mock-api на интеграцию с настоящим LND для генерации реальных Lightning invoices.

## Completed

1. `docker-compose.yml` содержит рабочие конфигурации `bitcoind` (regtest) и `lnd` (v0.17.0-beta).
2. `mock-api/server.js` использует gRPC-клиент с TLS + macaroon для подключения к LND.
3. При запросе `/sun_sign` mock-api вызывает `addInvoice` через gRPC и возвращает реальный Lightning invoice в заголовке `WWW-Authenticate`.
4. Исправлен путь к proto-файлу: `lightning.proto` вместо несуществующего `rpc.proto`.

## Done Definition — Verification

1. `docker compose up` запускает bitcoind, lnd, mock-api — OK
2. mock-api соединяется с gRPC LND — OK (`Successfully connected to LND`)
3. `curl -i http://localhost:8080/sun_sign` возвращает 402 + реальный regtest invoice — OK

## Note

Задача была объединена с L402-MVP-008 (healthchecks) — оба блокера решались одновременно. Подробности root cause analysis см. в `task_log_L402-MVP-008_20260306.md`.
