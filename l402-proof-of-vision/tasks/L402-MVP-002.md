# [DONE][TASKLOG][P0] L402-MVP-002 - Implement and containerize mock-api service

**Date:** 2026-03-06
**Workspace:** `D:\Dev\CATMEastrolab`
**Status:** Completed

## Objective

Создать и упаковать в контейнер простой веб-сервис, который будет имитировать платный ресурс (`webMCP`).

## Problem Statement

1.  Для end-to-end теста L402 нужен "защищаемый" ресурс.
2.  Использовать полноценный `webMCP` для этого теста избыточно и сложно.
3.  Компонент должен быть контейнеризирован для работы в общей среде с остальными частями MVP.

## Scope

1.  Создание `package.json` для Node.js-приложения с зависимостью `express`.
2.  Создание `server.js`, запускающего Express-сервер.
3.  Реализация одного эндпоинта `/sun_sign`, возвращающего статический JSON.
4.  Создание `Dockerfile` для сборки Node.js-приложения в production-ready образ.
5.  Добавление сервиса `mock-api` в `docker-compose.yml` с инструкцией по сборке.

## Out of Scope

1.  Какая-либо бизнес-логика, кроме возврата статического JSON.
2.  Настройка HTTPS.
3.  Механизмы аутентификации или авторизации (это будет делать L402-шлюз).

## Done Definition

1.  Все перечисленные файлы (`package.json`, `server.js`, `Dockerfile`) созданы в `mock-api/`.
2.  Сервис `mock-api` определен в `docker-compose.yml`.
3.  Команда `docker compose build mock-api` в директории `l402-proof-of-vision` успешно завершается без ошибок.

## Implementation

1.  Создан `mock-api/package.json` с зависимостью `express`.
2.  Создан `mock-api/server.js`, который запускает сервер на порту 3000 и отвечает на GET-запросы к `/sun_sign`.
3.  Создан `mock-api/Dockerfile` на основе образа `node:18-alpine`, который устанавливает зависимости, копирует исходный код и запускает сервер.
4.  В `docker-compose.yml` добавлен сервис `mock-api`, использующий `build: ./mock-api`.

## Verification

1.  Выполнена команда `docker compose build mock-api` из корневой директории `l402-proof-of-vision`.
2.  Команда успешно завершилась, что подтверждает корректность `Dockerfile` и файлов проекта.
3.  Лог сборки показал успешное выполнение всех шагов, включая `npm install` и экспорт образа.

## Artifacts

1.  `l402-proof-of-vision/mock-api/package.json`
2.  `l402-proof-of-vision/mock-api/server.js`
3.  `l402-proof-of-vision/mock-api/Dockerfile`
4.  Обновленный `l402-proof-of-vision/docker-compose.yml`
5.  Docker-образ `l402-proof-of-vision-mock-api` в локальном репозитории.
