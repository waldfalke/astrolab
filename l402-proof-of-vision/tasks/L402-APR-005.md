# [TODO][TASK][P1] L402-APR-005 — End-to-end real Lightning payment test

## Goal
Полный E2E тест: agent → Aperture (402) → pay via Lightning → get resource. Задокументировать и сделать воспроизводимым.

## Problem Statement
Все компоненты должны работать вместе. Нужен один скрипт/команда, которая поднимает стек, фондирует каналы, и запускает агента.

## Scope
1. **`scripts/run-e2e.sh`:**
   - `docker compose up -d bitcoind lnd lnd-client`
   - Wait for healthy
   - `./scripts/setup-channel.sh`
   - `docker compose up -d aperture mock-api`
   - Wait for Aperture ready
   - `docker compose run --build client-agent`
   - Print results
   - `docker compose down`
2. **Обновить `README.md`** с новыми инструкциями для Phase 2.
3. **Обновить ожидаемый вывод** в README — реальный Lightning payment вместо dummy.

## Depends on
- L402-APR-001 (Aperture)
- L402-APR-002 (lnd-client + channel)
- L402-APR-003 (client-agent real payment)
- L402-APR-004 (clean mock-api)

## Done Definition
1. `./scripts/run-e2e.sh` на чистой машине (с Docker) проходит от начала до конца.
2. В выводе видно:
   - 402 Payment Required от Aperture
   - Реальный Lightning invoice (lnbcrt...)
   - Успешная оплата через lnd-client
   - Preimage
   - Полученный ресурс `{sign: "Leo", paid_resource: true}`
3. `lncli listinvoices` на lnd показывает SETTLED invoice.
4. `lncli listpayments` на lnd-client показывает SUCCEEDED payment.
