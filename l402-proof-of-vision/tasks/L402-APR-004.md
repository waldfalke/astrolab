# [TODO][TASK][P1] L402-APR-004 — Strip L402 logic from mock-api

## Goal
Убрать всю L402/LND логику из mock-api. Сделать его чистым backend, который просто отдаёт данные.

## Problem Statement
Сейчас mock-api сам подключается к LND через gRPC, создаёт инвойсы, и всегда возвращает 402. С Aperture это больше не нужно — Aperture делает L402, mock-api делает бизнес-логику.

## Scope
1. **Упростить `mock-api/server.js`:**
   - Удалить gRPC-подключение к LND
   - Удалить `createInvoice()`, macaroon/TLS код
   - Endpoint `/sun_sign` просто возвращает 200 + JSON
2. **Упростить `mock-api/package.json`:**
   - Удалить `@grpc/grpc-js`, `@grpc/proto-loader`
3. **Удалить `mock-api/protos/`** — больше не нужны
4. **Обновить `docker-compose.yml`:**
   - Убрать `lnd_data` volume из mock-api
   - Убрать `depends_on: lnd` (mock-api зависит только от сети)

## Depends on
- Ничего (можно делать параллельно с APR-001/002)

## Done Definition
1. `curl http://localhost:3000/sun_sign` возвращает `200 OK` + `{sign: "Leo", ...}`.
2. mock-api не имеет зависимостей от LND.
3. Через Aperture: `curl http://localhost:8081/sun_sign` возвращает 402 (Aperture gate).
